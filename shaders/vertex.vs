#version 450

layout (location = 0) in vec3 pos;
layout (location = 1) in vec3 tCoord;

out vec3 texCoord;

// TODO: add uniforms if needed
uniform mat4 model;

void main() {
    // TODO: set gl_Position and texCoord
}